{
  "buildCommand": "cd ../.. && pnpm build --filter=employee-dashboard",
  "outputDirectory": "dist",
  "installCommand": "corepack enable && pnpm install --no-frozen-lockfile",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}